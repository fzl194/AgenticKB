# 51号批次3 旧表退役——部署验收清单（2026-09-18）

基线：批次2 部署完成后 → 本分支 `mcp-batch3-closure`（T1-T5）。
本批性质：**不可逆删表**（`mcp_access` / `mcp_open_kbs`），门禁与回滚约束
均按 51号 spec §7.2 批次3 裁定执行。

## 1. 前置条件（硬门禁，逐条勾选后方可进入 §3）

1. - [ ] **批次1 已部署完成**：apply + 三服务重启 +
     `backfill_user_domains.py` 迁移三连结束，批次1 清单五项验收全部通过。
2. - [ ] **批次2 已部署完成**：apply + mining/mcp_server 重启完成；
     `backfill_mcp_keys.py` 迁移三连结束（复跑 `migrated=0`）；
     批次2 清单五项验收全部通过（含 4.5 双钥匙域隔离——两把不同域
     钥匙互不可见）。
3. - [ ] **回滚窗口已过（业务侧确认）**：业务方书面/口头确认不再考虑
     回退到批次2 之前的状态。**删表不可逆**——旧钥匙数据一旦删除无法
     恢复（spec §7.2 显式裁定：用户重建即可，可接受）。此条未确认前
     **禁止执行 §3 删表**。

## 2. 部署

- 打包：`deploy-sync.sh pack`（范围含 `knowledge_mining/` 等；
  `drop_legacy_mcp_tables.py` 在仓库根，随包带上）。
- apply：`deploy-sync.sh apply`。
- **只需重启 mining 一个服务**（nginx 不动）：本批改动为 pg_schema
  注册表变更（008/010 两条迁移不再注册/不再重放），随 mining 启动生效。
  - serving（Java）本批零改动——不重启；
  - mcp_server 本批零改动——不重启；
  - main_control 本批零改动——不重启。
- **前端无变更**：kb-ui 不出包、不回退。

## 3. 删表（mining 重启完成后）

**唯一入口＝本脚本**（reset 工具的重置清单已不覆盖旧三表，不能替代本脚本）：

```bash
# 连接参数走环境变量 PG_HOST/PG_PORT/PG_DBNAME/PG_USER/PG_PASSWORD

# 1) 干跑，人工复核
python drop_legacy_mcp_tables.py --dry-run
# 重点看：
#   - 三表（mcp_access / mcp_open_kbs 及关联）行数是否符合预期
#   - 门禁判定：各条门禁是否全部放行（含门禁 a＝新钥匙体系就绪、
#     门禁 b＝计数比对 keys ≥ access）

# 2) 实删（mcp_open_kbs → mcp_access 顺序，显式 RESTRICT，单事务）
python drop_legacy_mcp_tables.py --yes

# 3) 复跑确认已退役
python drop_legacy_mcp_tables.py --yes
# 期望：报告「已退役/不存在」，退出码 0
```

**退出码约定**：

| 退出码 | 含义 |
|---|---|
| 0 | 完成，或无事可做（表已退役） |
| 1 | 门禁拒绝或执行出错——必须排查后重跑 |

**门禁 b 被拦截时**：先跑 `backfill_mcp_keys.py --dry-run` 复核迁移
状态；确认存量已全部迁移、确需强制删除时才用 `--force`。

> ⚠️ **`--force` 为高危操作**：仅在人工确认门禁误报/业务显式接受
> 丢弃未迁移数据时使用，使用前留存 dry-run 报告备查。

## 4. 验收 checklist（四项 + 一附加）

### 4.1 旧表物理不存在

- [ ] `SELECT to_regclass('mcp_access'), to_regclass('mcp_open_kbs');`
  两列均返回 NULL。

### 4.2 UI 钥匙功能无回归

- [ ] UI「MCP 钥匙」页：钥匙列表正常展示；
- [ ] 新建钥匙（选域+勾开放库）正常；
- [ ] 既有钥匙的配置查看/编辑正常。

### 4.3 检索链路无回归

- [ ] 任取一把钥匙调用一次 `search_knowledge`：正常返回结果
  （不报错、不空结果异常）。

### 4.4 迁移报告留档

- [ ] 批次2 迁移三连的 dry-run/实跑/复跑输出已归档
  （含被丢弃跨域勾选清单 dropped_cross_domain 明细），供后续追溯。

### 4.5（附加）迁移脚本退役自洽

- [ ] 复跑 `python backfill_mcp_keys.py`：显示
  「已退役/不存在，无需迁移」，退出码正常（非 1）。

## 5. 回滚说明（spec §7.2 批次3 裁定）

1. **删表执行后回滚窗口即关闭**：若必须回退到批次2 之前的状态，
   `mcp_access` / `mcp_open_kbs` 中的**旧钥匙数据不可恢复**——用户需
   在旧代码下重建钥匙。此为 spec §7.2 显式可接受项，回退公告中需说明。
2. **代码回退本身仍可行**：git revert 本批提交可恢复 008/010 的
   pg_schema 注册；回退后重启 mining，启动时按注册清单重放重建
   **空表**（结构在、历史数据无）。
3. 因此 §1 第 3 条（业务确认回滚窗口已过）是删表的唯一软保险，
   必须留存确认记录。

## 6. 附录：族外失败复核结论（Task 5，2026-09-18）

### 6.1 族外 15 失败定点复跑

对批次2 §0.1 登记的 15 个族外失败按文件族复跑（含
`tests/test_mining_auto_queue.py` 作回归哨兵，同场 6 文件 70 passed）：

| 文件族 | 批次2 数量 | 本次 | 裁定 |
|---|---|---|---|
| tests/test_v14_domain_pack.py | 11 | 11 精确复现 | 与本批 diff（docs/008/010 删除/pg_schema/reset/backfill/drop 脚本）零交集，域包/模板零改动，**维持环境性** |
| tests/test_offline_deployment_contract.py | 1 | 1 复现 | 依赖离线构建产物（版本化压缩包+校验和），本机无产物，**维持环境性** |
| tests/test_deployment_auth_surface.py | 1 | 1 复现 | 断言服务端口 loopback 绑定，依赖本机实际监听状态，**维持环境性** |
| tests/onenet/test_routes.py | 1 | 1 复现 | 一张网接线断言，本批未触，**维持环境性** |
| tests/parse_adapters/test_native_xlsx.py | 1 | 1 复现 | IR 链指纹断言，本批未触，**维持环境性** |

结论：15/15 与批次2 登记完全一致，无例外；`test_mining_auto_queue`
16/16 绿（上批补桩修复未回退）。零真实回归，未修任何业务代码。

### 6.2 全量回归基线（三组）

| 组 | 结果 | 与批次2 终态比对 |
|---|---|---|
| mining 全量（tests/kb + tests） | 2011 passed / 44 failed / 23 skipped（23:41） | 44 = 29 既知 flake 族 + 15 族外环境性，逐条对名一致；**族外新增 = 0**；test_mining_auto_queue 全绿 |
| mcp_server/tests | 86 passed / 0 failed（2.80s） | 全绿持平 |
| kb-ui npm run build | 成功（仅既有 chunk>500kB 警告，21.1s） | 通过（本批 kb-ui 零改动，基线闭环） |

注：passed 由批次2 的 2000 升至 2011，差异来自本批 T2 新增
test_pg_schema_kb 断言与目录集自然变化；failed 集合逐名比对无新增、无消失异常。
