# 02 · P02+P09 质量可观测真实运行时测试报告

> 日期：2026-08-25
> 分支：`codex/fix-p02-p09-quality-observability`（e038d88，基于 master 5ffb944）
> 方式：独立 worktree + 主区凭据同构部署（new_db 生产库）+ 真实挖掘 E2E
> 结论：**交付质量高，双通道指标落库真实验证通过，已合并**。测试过程中另修复 master 遗留 1 项、发现上游 bug 2 项（均非本分支范围）。

## 一、交付审查（e038d88）

改动与 02 号方案 S1 对齐：
- **基准补齐（P02 一半）**：`text/markdown`、`text/plain` 复用已读取的冻结源字节构造 `char_coverage` 基准（`_text_baseline`），零额外对象存储读取
- **落库双通道（P09）**：
  1. `asset_parse_runs.metadata_json.quality_attempts[]`（逐 attempt：index/parser/decision/issues/完整指标）
  2. `asset_document_snapshots.metadata_json.quality_metrics`（成功快照带完整指标）
- **只观测不强 gate**：自动基准不触发 FAIL/FALLBACK（显式传 source_text 的既有强制契约不变）——理由充分（单后端生产链防历史归一化差异批量误判）
- merge 语义正确：应用层读-合-写保留 `mode`/`plan_id` 等既有键（单写者无竞态）

交付件声称的 48 项单测**全部真实通过**（复跑验证），范围声明诚实（PDF/Office 基准、P02 fallback 留 S2/S3）。

## 二、运行时验证（new_db 生产库，真实挖掘）

流程：建 KB → 设范式 → 上传 txt → force_redo 挖掘 → `completed`（18 轮轮询）。

| 断言 | 修复前现状 | 实测 |
|---|---|---|
| parse_run `quality_attempts[]` | 22 run 零落库 | ✅ `decision:"PASS"` + `parser_id` + 完整指标 |
| **`char_coverage`** | 算不出（无 source_text） | ✅ **1.0**（源字节基准真实工作） |
| `evidence_locatability` | — | ✅ 1.0 |
| 快照 `quality_metrics` | 无此键 | ✅ char_coverage=1.0，m4-commit 其余键无损 |
| 旧 metadata 保留 | — | ✅ `mode`/`plan_id` 未被冲掉 |
| 回归（P13/P07/P03） | — | ✅ 匿名/伪造头/verify 401；本轮启动无滞留；freeze 链工作 |

## 三、过程中处置的其他问题

1. **master 遗留回归（已修，806c2b8）**：交付件自曝 `test_m6_facades` 3 failed——实为 **P03 合并时漏网**（旧夹具未注入 `FrozenInputService` → 门面一律 SKIP）。已在 master 修复夹具（7 项全过）。
2. **新发现上游 bug A（.md mime 误判，P01 范畴，未修）**：上传 `.md` 文件 mime 被记为 `application/octet-stream` → parser 拒收（`UnsupportedFormat`）→ 挖掘失败。`.txt` 正常（text/plain）。建议归入 P01 上传链整改。
3. **新发现上游 bug B（失败文档 run_doc 卡 processing，既有，未修）**：文档级解析失败时 `mining_run_documents` 不回写终态（P03 测试时已见同现象），Run 级 finalize 失败信息晦涩。建议与 P04 一并整改。

## 四、判定

P02+P09 S1（e038d88 + master 806c2b8）**验证通过，已合并**。质量指标从「算了没有、落了没有」双空转变为可观测双通道落库；文本类覆盖率基准真实生效。
