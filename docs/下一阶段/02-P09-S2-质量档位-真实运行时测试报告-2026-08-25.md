# 02 · P09-S2 质量档位真实运行时测试报告

> 日期：2026-08-25
> 分支：`codex/fix-p09-quality-profile`（86e2b45 原始交付 + strict 硬阈值修复）
> 方式：独立 worktree + 主区凭据同构部署（new_db）+ 容器内档位断言 + 145 项回归
> 结论：**交付声称的 strict「证据可定位率 0.90」是纸面阈值，已修复为真实硬门槛后验证通过，合并**。

## 一、交付审查（86e2b45）

- `qualityProfile` 三档（default/strict/lenient）经 `DocumentParseOptions`（workflow 节点参数）→ `plan_factory` 冻结进 `ParsePlan`，未知值 `__post_init__` 编排前拒绝 ✓ 链路闭合
- 每次 backend attempt 按冻结档位生成 Gate（default 保留注入 Gate，行为零变化）✓
- `max_backend_attempts` 契约默认 2→3 统一 ✓
- 声称的 57 项单测复跑全部真实通过 ✓

## 二、测试发现（MEDIUM，已修复）

### strict 的 locatability 门槛无实际约束力

- 档位表宣称 strict `evidence_locatability=0.90` 入库门槛，但 `gate.py:166-174` 对该维度违规**只降 WARN 永不 FAIL**——阈值仅停留在参数表。
- 连带发现既有降级缺陷：空容器分支（budget=None 兜底路径）无条件 `decision="WARN"`，会洗掉任何已置的 FAIL（历史上 FAIL 均提前 return 故无害，strict 硬阈值使其成为真实路径）。
- **修复**：`QualityProfile.hard_evidence_locatability`（仅 strict=True，default/lenient 维持 S1 只观测灰度承诺）；空容器兜底改条件式不降级既有结论。行为级测试锁定：同一指标向量（locatability=0.8）default 不拒、strict **FAIL**。

## 三、运行时验证（new_db，修复版镜像）

| 断言 | 结果 |
|---|---|
| strict/lenient/default 三档真实文档 parse | 全部 OK（正常文档 coverage=1.0 全档通过；同 snapshot 复用顺带验证内容寻址幂等）✅ |
| 未知档位 `bogus` | 编排前 ValueError 拒绝 ✅ |
| 回归（parse_quality/contracts/m6/operator/multi_format/shadow/frozen_input） | 145 passed + 1 skipped ✅ |
| S1 双通道落库 | 不受影响（本分支未触碰落库路径）✅ |

注：运行时制造低 locatability 真实文档不可控（txt 解析锚定恒优），strict FAIL 的运行时终态表现依赖单测行为级锁定 + 单次 attempt 语义（P02 S3 fallback 链未接，FAIL 即文档失败）。

## 四、判定

P09-S2（86e2b45 + strict 硬阈值修复）**验证通过，已合并**。三档从 UI 参数落为真实 Gate 行为；strict 的门槛承诺做实。
