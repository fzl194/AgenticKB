# 03 · P03 冻结输入真实运行时测试报告

> 日期：2026-08-25
> 分支：`codex/fix-p03-frozen-input-stale-check`（92fa2fc 原始交付 + b771397 import 修复）
> 方式：p03 worktree 独立镜像 + kb_db 真库 E2E（上传→挖掘→快照）+ master 对照实验
> 结论：**原始交付为半成品（真实挖掘全断），已修复 import 错误后验证通过并合并**。

## 一、测试发现（CRITICAL，已修复）

### 92fa2fc 的 ImportError 使全链挖掘静默断链

- `_parse` 内 `from knowledge_mining.mining.frozen_input.contracts import StorageObjectMissing`——该符号实际在 `contracts.storage.errors`。
- ImportError 在函数内触发 → workflow 文档级异常处理吞掉 → 无任何 traceback → 终态表现为晦涩的 `Cannot finalize before capabilities: assets_persisted`，`mining_run_documents` 卡 `processing`。
- **真实运行时对照实验**（同库同配置同数据，唯一变量为代码）：
  | 代码 | Run 结果 | run_doc | 快照 |
  |---|---|---|---|
  | master(8ce0e80) | `completed`（约 3 分钟） | `committed` + document_id 关联 | 产出 |
  | 92fa2fc | `failed`（18 秒） | 卡 `processing`、document_id=None | 无 |
- **单测假绿陷阱**：交付时声称的 12 项全链测试通过，实为跑在 codex 未提交的「回滚态」工作区上（等于测 master 代码）。纯净 92fa2fc 上 4 failed。
- 修复（`b771397`）：import 路径一行修正。修复后 frozen_input(55) + 全链(24) 单测全过。

## 二、修复后验证

### freeze 链（容器内直调，逐步断言）

| 步骤 | 结果 |
|---|---|
| `documents.get` → current | `storage_object_id`/`source_raw_hash`/`content_revision` 正确 ✅ |
| `storage_objects.get` → record | `AVAILABLE`、bucket/key 正确 ✅ |
| `svc.freeze()` → FrozenInput | **captured_at=真实时间**（非 1970）、mime/size/object_key 正确 ✅ |

### E2E（kb_db 真库：上传 txt → 建范式 → force_redo 重挖）

- Run `completed`（约 3 分钟，与 master 等时）✅
- `mining_run_documents` → `committed` + document_id 关联 + snapshot 产出 ✅
- `asset_parse_runs` → `legacy_txt` `SUCCEEDED` ✅
- `check_stale` 已替换 `_no_stale` 空函数（组合根 wiring）✅
- SKIP 语义保留（StorageObjectMissing → None，单测覆盖）✅

## 三、测试现场处置

- 测试容器曾按 worktree git 版配置连 kb_db（非生产 new_db）——KB `rj_test` 与测试文档留在 kb_db（旧库，无害）。
- p03 worktree 内 codex 未提交的「大规模回滚改动」（撤销 P03/P07/P13，含删 deploy-server.sh/auth_guard.py）已 `git stash` 保存未销毁——**勿盲目 pop**，建议废弃（`git stash drop`）。
- 测试后主区已恢复 master 镜像与 new_db 配置部署。

## 四、判定

P03（92fa2fc + b771397）**验证通过，已合并 master**。冻结输入从「手搓 1970 + `_no_stale` 空转」变为真 freeze 链 + 真 stale 校验，且挖掘主路径与 master 行为等价。
