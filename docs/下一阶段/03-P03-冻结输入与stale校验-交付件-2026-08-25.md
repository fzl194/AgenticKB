# 03 · P03 冻结输入与 stale 校验：S1 交付件

> 日期：2026-08-25
> 范围：P03-S1；不包含 P11 路径输入与临时文件治理。

## 交付结果

- Workflow 解析门面改为调用 `FrozenInputService.freeze()`，不再手工构造固定时间戳的冻结输入。
- Snapshot 提交服务注入真实 `FrozenInputService.check_stale`；文档内容在解析期间变化时，既有解析器机制会将 Run 置为 `SUPERSEDED`，不提交过期 Snapshot。
- 保留旧链或缺少对象注册文档的 SKIP 语义，避免将历史兼容场景升级为 Run 级失败。
- 冻结输入补回当前文档展示名，避免 Snapshot 标题退化为空。

## 验证

- `new_chain/test_multi_format_pipeline.py`、`parse_operator/test_document_parse_service.py`、`frozen_input/test_frozen_input_service.py`：26 项通过。

## 边界

- legacy revision 为 NULL 的存量补数属于 P03-S4，不在本次。
- 受控临时文件、path 输入和内存治理属于 P11/P03-S2，不在本次。
