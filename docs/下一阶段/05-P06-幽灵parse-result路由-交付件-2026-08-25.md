# 05 · P06 幽灵 parse-result 路由交付件

> 日期：2026-08-25
> 范围：移除未挂载的旧 `/api/knowledge/.../parse-result` 路由与前端死方法。

## 交付结果

- 删除未挂载的 legacy parse-result router；生产 KB 路由不受影响。
- 删除 `useMiningApi().getParseResult` 死方法。
- 保留 `ParseResult` TypeScript 接口，KB 新路由仍以它作为返回类型。

## 验证

- 旧路由此前未在应用组合根注册；删除后无生产 import 或调用方。
