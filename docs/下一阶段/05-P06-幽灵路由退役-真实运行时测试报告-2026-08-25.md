# 05 · P06 幽灵 parse-result 路由退役测试报告

> 日期：2026-08-25
> 分支：`codex/fix-p06-parse-result-retirement`（f08da16，基于 8ce0e80）
> 方式：静态引用核查 + 单测 87 项 + 镜像构建（含 UI 编译）+ 运行时断言
> 结论：**改动干净，验证通过，已合并**。

## 一、改动核验（f08da16）

删除三处死代码：
- `mining/api/routes/parse_result.py`（`GET /api/knowledge/documents/{id}/parse-result`）
- `kb-ui/src/api/mining.ts` 的 `getParseResult` 方法
- 附交付件

**三炸点核查全过**：
| 疑点 | 核查结果 |
|---|---|
| app.py 是否 import 已删文件 | 无任何引用（含动态 import 字符串）——路由从未被 include，真·幽灵 ✅ |
| kb-ui 是否有 getParseResult 活调用 | 全仓唯一引用即被删的方法定义本身 ✅ |
| `test_parse_result_route.py` 是否 import 已删模块 | import 的是 KB 侧活路由 `kb/routes/documents.document_parse_result`，不受影响 ✅ |

## 二、测试结果

- 后端单测 87 项通过（parse_result 路由测试 + 鉴权 guard 含全路由枚举 + P07 恢复 + frozen_input）✅
- 镜像构建通过（**含 kb-ui 前端编译**——`getParseResult` 删除无编译断裂的实证）✅
- 部署六服务健康 ✅

运行时断言（kb_db，复用 P03 测试产出的 KB/文档/快照）：

| 断言 | 结果 |
|---|---|
| 幽灵路由 `GET /api/knowledge/documents/{id}/parse-result`（合法身份） | **404**（退役生效）✅ |
| KB 活路由 `GET /api/kb/{kb}/documents/{id}/parse-result` | **200**（未误删，P03 快照可读）✅ |
| UI 首页 | 200 ✅ |
| P13 回归：匿名 `/api/kb` | 401 ✅ |
| 匿名打幽灵路径 | **401**（鉴权先于路由判定，纵深正确）✅ |

## 三、判定

P06 是零行为变化的死代码清理：幽灵路由从未接线，删除前后运行时等价（实证而非推断）。已合并 master。
