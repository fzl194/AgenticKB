# 09 · 批次1 止血与速赢 真实运行时测试报告

> 日期：2026-08-27
> 分支：`fix/batch1-stopgaps`（我直接实施；5 个 bug + 对抗测试中追加修复 1 项）
> 方式：TDD 单测（Python 24 项新增 + Java mvn 383 项）+ worktree 独立镜像部署 + 真实运行时对抗 E2E（跨栈软删矩阵）
> 结论：**五项全部通过运行时验证，合并 master**。

## 一、修复清单与验证结果

### BUG-1 P08-S1 软删替代硬删（P0，Python + Java 双栈）

原 `DELETE FROM asset_documents` 借 FK CASCADE 抹掉 `asset_build_document_snapshots`——每次删文档都在**改写历史 Build**。

运行时跨栈不可见矩阵（new_db 真库，KB 挖掘完成后软删 PDF 文档）：

| 断言 | 结果 |
|---|---|
| Python 列表不含已删文档 | ✅（只剩 t.md） |
| 详情 / 下载 404 | ✅ / ✅ |
| **Java `selectLatestKbSnapshots`（DISTINCT ON 原文执行）排除已删** | ✅ |
| Java scope 保留未删文档 | ✅ |
| 身份行仍在（deleted_at 盖章，非物理删除） | ✅ |
| **历史 Build 的 selection 行完整（核心修复）** | ✅ |
| restore 后全端恢复可见 | ✅ |
| 同名重传复活（同 id、revision+1、无 409） | ✅ |

对抗测试追加修复 1 项：revive SQL 同一参数喂 TEXT（modified_at）与 TIMESTAMPTZ（content_updated_at）两列触发 `AmbiguousParameter`——`::timestamptz` 转型无效（PG 把转型绑定到参数本身），必须拆独立参数。mock 单测盲区，运行时实锤。

Java 侧：`AssetBuildDocumentSnapshotMapper`（KB scope 核心查询）+ `AssetDocumentMapper`×2 + `AssetRawSegmentMapper.selectSegmentSources` 加 `d.deleted_at IS NULL`；mvn test 383 项全过。

### BUG-2 `.md` mime 误判

上传声明 `application/octet-stream` 的 t.md → storage object 落库 **text/markdown** ✅（扩展名回落；具体声明优先）。覆盖单文件与 zip 两条上传路径。

### BUG-3 失败文档 run_doc 终态回写 + 计数黑洞

挖 corrupt PDF + 正常 md：run `completed`，**total=2 / committed=1 / failed=1 / skipped=0——计数闭合**（修复前 failed=0、两个文档消失）。run_doc 明细：`doc:/t_bad.pdf` → `failed`（带 parse run 失败信息）、`doc:/t.md` → `committed` ✅。

三层修复：executor 终态分支（FAILED+SKIP_DOCUMENT→failed / 真跳过→skipped，经 services sink）+ 链尾未 commit 兜底 + run 级失败批量清扫。

### BUG-4 resume 已 finalize Run 终态回写

completed run 手动拨 `interrupted` → resume → **最终态 completed 落库** ✅（修复前快速路径 ~4s 内存返回、行永滞 running、每次重启回弹）。

### BUG-5 批次挖掘通道退役（用户决策：挖掘必须 KB 化）

运行时断言：`POST /api/runs` → **405**、`POST /api/uploads` → **404**、`POST /api/runs/preflight` → **405** ✅。KB 挖掘入口不受影响（全程 E2E 均走 `/api/kb/{id}/mine`）。UI 批次 API 方法为死代码（无视图引用），连同类型/spec 一并清理；`npm run build` 通过。

## 二、测试现场处置

- 测试 KB `b1_e2e` 已软删（级联文档）；3 条测试 Run 已清理。
- worktree 凭据 config 已还原 git 版。

## 三、遗留与边界

- 域级（kb_id=NULL）legacy 文档的 `document_lifecycle`/`knowledge.py` 读面未加过滤——软删只作用于 KB 文档，legacy 路径无删除入口，不构成泄漏面（P05/P08-S2 批次再收口）。
- P08-S2（KB withdrawal removal Build）与 P08-S3/P12（purge/GC）按批次计划后置。
- 批次通道退役后 `resolve_upload_batch_path` 已无消费者（随 uploads.py 删除）。

## 四、判定

批次1 五项全部验证通过，`fix/batch1-stopgaps` 合并 master。
