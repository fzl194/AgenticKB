# 13 · 批次3 读路径与体验加固 真实运行时测试报告

> 日期：2026-08-27
> 分支：`fix/batch3-readpath-auth`（我直接实施；三问题齐修）
> 方式：TDD（认证 6 + 读路径 4 + 守卫 4 项新增）+ 全量回归 1273 passed + worktree 部署 + 生产库 E2E（含并发压测）
> 结论：**三项全部通过运行时验证，合并 master**。

## 一、修复与验证对照

### 问题1 认证挤兑（多人用就随机 401/500）

| 项 | 结果 |
|---|---|
| **30 并发 × 30 秒认证压测（生产库）** | **1580 请求，0 次 401 / 0 次 500** ✅（此前同场景实测必现失败窗口） |
| 身份确认缓存 | 60s TTL + 500 条上限；命中零触库（单测）✅ |
| 禁用即时生效 | users PATCH 端点同进程主动 invalidate（单测 + 既有 auth 路由测试回归）✅ |
| 连接池 | 2/10 → 4/20（git 默认值 + 部署机凭据版同步）✅ |

### 问题2 大文档知识页整包（读路径三重全量）

| 项 | 结果 |
|---|---|
| 知识读限量 | 四类查询 LIMIT 2000 + 窗口函数精确总数；E2E 响应携带 `truncated/total_segments` 新字段 ✅ |
| IR 内存副本 | 攒 chunks+join+loads+from_dict 4 份 → 磁盘直解 2 份（tracemalloc 峰值断言）✅ |
| 快照 IR 缓存 | 不可变对象 LRU 16 条；二次读零触库零拷贝（单测）✅ |

### 问题3 扫描件守卫接线（file_inspector 落地，"能力在库接线为零"第 4 例）

| 项 | 结果 |
|---|---|
| E2E：挖 1 文本 PDF + 1 扫描 PDF | run completed：text **committed**、scan **failed** 且错误信息为 `scanned_pdf_needs_ocr: 扫描件 PDF（无文本层）…需 OCR（未上线）`——**直指根因**（修复前是误导性的 `empty_document`）✅ |
| 守卫路径留痕 | run + attempt 完整（状态机合法 PARSING→FAILED）；检测结果按 source 对象缓存 ✅ |
| 不误伤 | 文本版 PDF/加密 PDF/非 PDF 均放行（单测）✅ |

## 二、其他

- 主区 database.yaml（凭据版）同步池 4/20——skip-worktree 文件不随 git 走，已手动同步。
- 部署机旧 JWT 因批次2c 密钥轮换已全部失效，属预期。

## 三、判定

批次3 三项验证通过，`fix/batch3-readpath-auth` 合并 master。
