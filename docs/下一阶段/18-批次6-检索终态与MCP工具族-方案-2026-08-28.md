# 批次6/7「检索终态 + MCP 工具族」方案（2026-08-28，待审查）

> 用户 2026-08-28 指令定稿：知识库内检索 tab + 老检索全线下线 + MCP 独立页与工具族。
> 上一批（批次5）报告见 17 号文档；本方案替代原"批次6 删除链/批次7 工具族/批次8 链路统一"的顺序——用户将删链路提前。

---

## 0. 目标形态（用户口径）

**检索只有一个世界：知识库 × 检索范式。**

```
左侧导航                          知识库详情
├─ 知识库                          ├─ 文件
├─ MCP 接入（新，全员）             ├─ 检索（新）：选/绑本库检索范式
│    ├─ Agent 接入配置 JSON 一键复制 │    + 搜索框直接测试本库检索
│    ├─ 密钥（一人一个）            ├─ 成员
│    ├─ 开放哪些 MCP 工具（开关）    ├─ 挖掘
│    ├─ 检索工具开放哪些库（勾选）   └─ 设置
│    └─ 工具提示词/描述（可编辑）    （旧"检索"左侧菜单删除；老固定链路删除）
```

- 每个知识库怎么检索 = 库自己绑的范式决定（设置/检索 tab）
- MCP 是用户级：工具开关 × 开放库 × 提示词，全在 MCP 页自管
- **检索只剩范式链路**：网页端测试、MCP、未来一切调用全走范式执行

## 1. 批次6：检索终态（先做）

### 1.1 知识库"检索" tab（kb-ui）
- 顶部：本库检索范式选择器（= 库级绑定 default_paradigm_id 的快捷入口，含"跟随官方默认"）
- 中部：搜索框 + 检索按钮（调试开关可选）；执行走 `POST /api/v1/paradigm/{id}/search`，`kbIds=[本库]`，身份为当前用户
- 结果区：证据列表（标题/文本/来源文档/得分），命中为空时提示"先挖掘出检索单元"并链到挖掘 tab
- 附：当前生效管线说明（"本库绑定：xxx" / "官方默认"），以及 scope 留空范式 + 本库注入的提示

### 1.2 老检索下线（不可逆，用户已拍板）
- kb-ui：左侧"检索"菜单与 SearchView **删除**
- Java：`SearchService` 九步固定链路与 `POST /api/v1/search` **删除**（连同 Controller）；FTS 降级链、RetrievalRouter、树导航、语义缓存、多查询扩展等固定链路专属组件随之退役或转入算子复用清单
- MCP 侧：批次5 已删回落，无改动
- **代价（已向用户说明）**：老链路独有的四个能力（树导航/语义缓存/多查询扩展/级联重排）尚未算子化——官方默认范式是"向量+关键词+融合+重排"的基础检索，复杂问题召回质量可能弱于老链路。四能力算子化转为后续增强批次（批次8+），不再阻塞删链路。
- 灰度项：无（用户明示直接切）。检索日志（serving_query_logs）继续记录，效果回归靠查询日志对比观察。

### 1.3 域绑定退役（上轮已确认）
- 解析简化为三层：显式 > 库级 > 官方默认（无域默认层）
- Java：`operator_paradigm.bound_domain/is_default` 与绑定端点/校验链移除；resolve 删 domain 层；`/mcp-catalog` 的域过滤改"全库通用"
- kb-ui：范式列表页"绑定知识域/设为默认"入口删除
- 存量数据：控制库无域绑定行，零迁移

### 1.4 检索范式管理的入口保留
- 左侧"范式管理"页保留（创建/编辑/发布范式本身不变）；列表上去掉绑定列与 Agent 可见性（改由 MCP 工具开关决定）

## 2. 批次7：MCP 工具族与管理页（紧随批次6）

### 2.1 MCP 独立 tab（kb-ui，全员可见）
自上而下：
1. **Agent 接入配置 JSON**：按当前部署地址 + 本人密钥拼好的完整配置（MCP streamable-http 端点 + Authorization 头），一键复制；提供 dify/通用两种格式
2. **密钥**：状态/重新生成（旧钥立即失效）
3. **工具开关**：每个工具可单独开放/关闭（默认全开）
4. **检索工具的开放库**：勾选（检索范围=开放库 ∩ 本人权限）
5. **提示词与工具描述**：MCP 对 Agent 的自我介绍（instructions）+ 每个工具的描述文字，可编辑（留空=默认文案）；生效于 tools/list 动态渲染

### 2.2 MCP 工具族六件套（mcp_server）
| 工具 | 说明 | 数据源 |
|---|---|---|
| search_knowledge | 检索（已有，按开放库路由） | serving 范式执行 |
| list_knowledge_bases | 我开放的库清单（id/名称/文档数/就绪度） | mining KB API（新增内部端点） |
| list_documents | 某库的文件列表（分页，名称/状态/大小） | mining KB 文档 API（新增内部端点） |
| get_document | 某文件的结构化内容（切片级） | mining 知识读路径（新增内部端点，软删过滤） |
| get_segment_fulltext | 切片原文（已有） | serving |
| upload_document | 上传文件入库（走批次2b/2c 上传通道；**不自动触发挖掘**，页内提示去界面点挖掘——自动挖掘与否留小决策） | mining KB 上传 API |

- 全部工具强制密钥；工具开关在鉴权响应里下发（verify 返回 open_tools），未开放的工具不出现在 tools/list、调用即拒
- **tools/list 动态化**：按用户配置渲染（开关×描述文案）——需把 FastMCP 1.x 静态注册改造为自定义 list_tools/描述注入（批次5 遗留的"tools/list 匿名可见"一并收口：无钥连清单都拿不到）

### 2.3 配置存储（Python 域库）
- `mcp_access` 扩列：`open_tools jsonb`、`instructions text null`、`tool_descriptions jsonb`
- verify 响应扩：open_tools/instructions/tool_descriptions（MCP 免二次查）

## 3. 实施与验收要点

- 批次6 验收：KB 检索 tab 可选范式/换范式/搜索命中；左侧检索菜单消失；`POST /api/v1/search` 404；mvn 删除链路后全绿（383 项中固定链路测试随删）；解析三层回归
- 批次7 验收：MCP 页 JSON 复制即用（真实 dify/通用客户端冒烟）；工具开关生效（关掉的 tool 不可见不可调）；描述修改后 tools/list 即时反映；六件套 E2E（含上传→界面可见→手动挖掘）
- 两批各自 worktree→TDD→E2E→报告（18/19 号续编）→合并，节奏连续

## 4. 待确认（默认已选，可改）

| # | 决策 | 默认 |
|---|---|---|
| D1 | 删老链路的四能力差距接受度 | **接受**（基础检索完整；四能力算子化为后续增强批次） |
| D2 | upload 工具是否自动触发挖掘 | **不自动**（Agent 上传后提示用户手动挖；防失控成本） |
| D3 | 批次6/7 连续做（先 6 后 7） | **是** |
