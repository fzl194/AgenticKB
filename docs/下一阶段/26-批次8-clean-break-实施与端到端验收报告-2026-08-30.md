# 批次8「Clean Break 挖掘×检索重构」实施与端到端验收报告（2026-08-30）

> 实施基线：[24 号（挖掘侧）](24-批次8-挖掘侧算子与配套范式-需求设计-2026-08-29.md)
> / [25 号（检索侧）](25-批次8-检索侧算子与Agentic工具-需求设计-2026-08-29.md)；
> 决策账本：[23 号](23-批次8-挖掘检索算子重审-决策与待讨论清单-2026-08-29.md)。
> 状态：**三波次（8a/8b/8c）全部实施、部署并在生产域库完成端到端验收**。
> master=`2de813a`（worktree `fix/batch8-clean-break` 分支级合并）。

---

## 1. 交付总览（波次 × 提交）

### 8a 契约与清理
| 提交 | 内容 |
|---|---|
| `eeda68c` M0 | 挖掘正式目录收口 6→7 算子；删 enrich/discourse/contextualizer/unit_build 注册；实体/本体七算子研究隔离（research.py 边界）；normalizer/compiler 自动注入路径删；停旧 seed；旧 7 模板清空；前端三菜单注释下线 |
| `703dfb7` M1 | 版本化 `MiningDocumentBundle`（引用+计数，序列化边界）；parse 直交 bundle；**删 to_raw_segment_data 兼容投影**；document_facts 确定性聚合 |
| `893ab93` M2 | `RetrievalRepresentation` 契约（11 型矩阵/canonical/eligibility）+ 纯投影器（table_row"列为值"/同源不同 canonical/includeSections 有界聚合）；catalog 第 7 算子 retrieval_unit_project；embedding 依赖恢复 |
| `67b36e3` R0 | Java 目录收敛 7 算子；删固定链（SearchService 九步链/QueryUnderstandingEngine/MultiQueryExpander/TreeNavigator/SemanticCache/pipeline 包/三 reranker）+10 退役算子；entity 双算子去注册；官方图停 seed |

### 8b 资产与召回
| 提交 | 内容 |
|---|---|
| `7b3607e` M4 | per-representation embedding policy（emb-policy-1 矩阵/覆盖/显式 fallback/capability 校验）；EmbeddingRecord 冻结 provenance；EmbeddingOptions→strategyOverrides |
| `468275e` M5 | 三面资产：structure_projection（nodes/edges/table cells）+ readiness 四能力（含 column_aggregability）+ AssetPersistService + v2 DDL（units_v2 tsvector GIN/embeddings_v2 VECTOR）+ persist handler bundle 化 + jieba 预分词契约（TOKENIZER_VERSION 冻结） |
| `c461a8b` R1-R4 | fts/dense 切 v2 表（lexical/dense eligibility/facets 下推/canonical 通道聚合/维度校验）；CJK 查询分词（jieba-analysis 同源）；scope hard filters；rrf channelWeights；model_rerank topN/类型化 ranking_text/降级保序 |

### 8c 增强与收口
| 提交 | 内容 |
|---|---|
| `fa299e9` M3 | query_expansion_generate（资格门/SKIP/answer_span 归一化回源/三层去重）+ hierarchical_summary_generate（标题树自底向上/derived）两实验算子（目录 7→9）；LLM 失败只 degraded |
| `0a9c4d4` M6 | 4 套挖掘预置（lexical/hybrid[默认]/query-alias/longdoc）seed 重建 + DEFAULT_WORKFLOW_ID=system-hybrid-assets |
| `8f92d3d` R5-R6 | evidence_hydrate（Top-N 批量回源/类型化展开 auto·window·parent·whole/alias 回源）；assemble 重写（canonical 合并/span 去重/预算/truncated+has_more）；**EvidenceResponse 极简协议**（顶层 query/evidence/has_more；opaque refs ev_/doc_/st_ HMAC 域分隔）；ContextPack 全族删除 |
| `08b7b7a`+`2226474` R7-R8 | structure_navigate（10 白名单关系/cursor）+ structured_query（schema-bound DSL/typed errors 8 错误码）+ inspect_knowledge + get_evidence/get_document（internal API + X-Internal-Auth）；两套检索预置重建（system-hybrid-retrieval[官方默认]/system-lexical-retrieval）；MCP 九件套（search/get_evidence/get_document/inspect/navigate/query_structured/list×2/upload）；kb-ui 渲染切 EvidenceResponse |
| `53e58f9` | PG 落库三件套（PgRepresentationStore/PgEmbeddingStore/PgAssetWriter）+ deploy 密钥注入（SERVING_EVIDENCE_REF_SECRET/SERVING_INTERNAL_AUTH_SECRET generate-once） |
| `feea1b0`/`efb3539`/`6cc350b`/`8ee7852`/`53d8406`/`2de813a` | 端到端联调修复：五件套服务绑定运行时、run_binding 兜底切新默认、文档身份闭环（parse 回填 document_id + persist 后 commit_document）、embedding 维度从真实向量回填、能力校验计数切 v2、mapper 对齐 raw 表实际列形态 |

## 2. 受保护 reset（24 号 §10.2/§10.3）

`tools/reset_domain_assets.py`（`f9b43ff`）：注册域匹配 + FK 完备性校验 +
保留白名单（21 表：用户/密钥/开放库/存储层/审计/LLM 观测）+ 显式确认 +
默认 dry-run。生产执行：DROP 旧形态 `asset_raw_segments` + TRUNCATE 29 张
派生/运行/范式/图谱/缓存表；control-plane 全保留（admin 密码/开放库配置
沿用）。

## 3. 端到端验收（生产环境 2026-08-30）

**链路**：登录 → 建库（绑 system-hybrid-assets 默认预置）→ 上传中文
Markdown（含表格）→ mine → 7/7 节点 completed → build validated →
doc committed（身份回写）→ 检索范式 API → MCP。

| # | 验证项 | 结果 |
|---|---|---|
| 1 | 挖掘 7 节点全绿（parse→compile→project→embed→persist→finalize） | ✅ |
| 2 | v2 资产落库：units_v2=2、embeddings_v2=2（1024 维）、structure_nodes=7 | ✅ |
| 3 | embedding policy 真实执行（LLM 服务向量，维度自动回填） | ✅ |
| 4 | finalize 能力校验过 v2 计数 → build validated + release-ready | ✅ |
| 5 | 检索（system-hybrid-retrieval）：中文"风扇停转怎么处理"→ **table 证据（A-101 行+表头+章节面包屑）** + prose 次命中；EvidenceResponse 协议（无内部 id/score） | ✅ |
| 6 | opaque refs（ev_/doc_/st_ 前缀 HMAC）+ source 溯源（KB/文件/章节） | ✅ |
| 7 | MCP tools/list 动态九件套（旧开关置 null 后全量） | ✅ |
| 8 | MCP search_knowledge → 纯 EvidenceResponse（"A-102 电源过载"命中表格行） | ✅ |

**测试基线**：knowledge_mining 1235+ passed（kb/* 真库门禁用例按
KB_RUN_POSTGRES_ACCEPTANCE 门控）；Java mvn 397 green；mcp_server 50
green；kb-ui build+254 green。

## 4. 遗留与后续（不阻塞验收）

| # | 项 | 定性 |
|---|---|---|
| 1 | 表格行拆分默认关闭（hybrid 预置 segment_compile 未显式 tableView=both → 整表一片，table_cells=0；结构化查询需 rows/both 档） | 建议下批把官方预置默认改 `tableView: both` 并重挖 |
| 2 | `asset_raw_segments` 双形态真相源（001 legacy 形态 vs schema.py v2 形态声明；mapper 已对齐实际形态） | 技术债：统一 001 DDL 或迁移 |
| 3 | hybrid-retrieval 预置 `is_default` 标记未置（解析走 id 兜底正确） | 小瑕疵 |
| 4 | kb-ui 旧 FullTextDrawer/fetchFullText 孤儿代码 | 清理项 |
| 5 | 质量评估套件（21 号 §5.4：问题集+新老对比）尚未建设 | 批次9 首项——按 24/25 号定位为开发验收之外的独立交付 |
| 6 | M3 两实验算子生产 LLM 接线（query_expansion/summary 的 generator 注入组合根）| 已有 facade+handler，缺生产 wiring |
| 7 | S5b fastmcp initialize instructions 注入限制仍在（25 号 §8.2 已注明降级语义） | 待 fastmcp 升级 |

## 5. 部署注意（新 env）

- `SERVING_EVIDENCE_REF_SECRET` / `SERVING_INTERNAL_AUTH_SECRET`：deploy
  首次生成入宿主机 `.env`（600），**generate-once 不轮换**；丢失=ref 跨
  重启失效/结构工具 503。
- deploy 脚本自带 MSYS 路径自防护（`1ca7e33`）；**Git Bash 下必须从仓库
  根目录跑**。
- 全服务重启后 admin 登录密码以 kb_users 为准（auth.yaml 的
  admin_password 仅 bootstrap 生效）。
