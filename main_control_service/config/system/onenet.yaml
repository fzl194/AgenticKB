# 知识一张网接入配置
# 凭据来源：内网 appid.txt（app_id / static_token 两行）——填完勿提交 git（同 llm_service.yaml 惯例）
app_id: 'com.example.app'
static_token: 'replace-with-static-token'
# 以下均可省略（默认值见 mining/onenet/config.py）
# token_url: 'http://oauth2.huawei.com/ApiCommonQuery/appToken/getRestAppDynamicToken'
# search_url: 'http://apigw-cn-south02.huawei.com/api/search/source_type?source_type=0'
# source_type: 0
# timeout: 300
# verify_tls: false
# chunk_width: 10000
# page_size: 1000
# throttle_seconds: 0.3
